# PlanoSnap

Proyecto independiente `PlanoSnap`.

Programa para tomar notas, medidas y cotas de objetos y espacios.

## Siguientes pasos

- Agrega tus archivos de proyecto aquí
- Usa `git add .`, `git commit -m "Iniciar PlanoSnap"` y `git push` cuando tengas un remoto configurado
